---
phase: 02-visual-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [popup/popup.css]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All UI elements use deep blue/charcoal as primary color"
    - "Accent color is teal (#0d9488) or soft blue (#3b82f6)"
    - "Background is white (#ffffff) or light gray (#f9fafb)"
    - "Design has clear visual hierarchy with primary actions emphasized"
    - "Layout uses generous whitespace for readability"
    - "Design is clean and professional (Linear meets modern job platform aesthetic)"
    - "Typography uses modern sans-serif font (Inter or system fonts)"
    - "Popup dimensions remain reasonable for browser extension"
  artifacts:
    - path: "popup/popup.css"
      provides: "Color system, typography, spacing, visual hierarchy"
      contains: "CSS variables for colors, spacing scale, typography system"
  key_links:
    - from: "popup/popup.css"
      to: "All UI components"
      via: "CSS custom properties (--color-primary, --color-accent, etc.)"
      pattern: "var\\(--color-|--spacing-|--font-"
---

<objective>
Establish a professional color scheme, typography system, and visual hierarchy for the TailorAI Chrome extension, replacing the current Apple-style blue (#007aff) with a trustworthy deep blue/charcoal primary and teal accent system.

Purpose: Transform the UI from functional to professional, matching the high-stakes job application context where users need to feel confident and trustworthy. The new design system will provide visual consistency, improved readability, and a modern aesthetic that aligns with professional job platforms like Linear.

Output: Updated popup.css with a complete design system including CSS variables for colors, typography, spacing, and visual hierarchy.
</objective>

<execution_context>
@C:\Users\tahas\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\tahas\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@popup/popup.html
@popup/popup.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CSS variable system for colors, spacing, and typography</name>
  <files>popup/popup.css</files>
  <action>
At the top of popup.css (after the * selector reset), add CSS custom properties following this pattern:

```css
/* ===== DESIGN SYSTEM ===== */

/* Color Palette */
:root {
  /* Primary - Deep Blue/Charcoal */
  --color-primary-50: #f8fafc;
  --color-primary-100: #f1f5f9;
  --color-primary-200: #e2e8f0;
  --color-primary-300: #cbd5e1;
  --color-primary-400: #94a3b8;
  --color-primary-500: #64748b;
  --color-primary-600: #475569;
  --color-primary-700: #334155;
  --color-primary-800: #1e293b;
  --color-primary-900: #0f172a;
  --color-primary-950: #020617;

  /* Accent - Teal */
  --color-accent-50: #f0fdfa;
  --color-accent-100: #ccfbf1;
  --color-accent-200: #99f6e4;
  --color-accent-300: #5eead4;
  --color-accent-400: #2dd4bf;
  --color-accent-500: #14b8a6;
  --color-accent-600: #0d9488;
  --color-accent-700: #0f766e;
  --color-accent-800: #115e59;
  --color-accent-900: #134e4a;

  /* Backgrounds */
  --bg-primary: #ffffff;
  --bg-secondary: #f9fafb;
  --bg-tertiary: #f3f4f6;

  /* Text */
  --text-primary: #0f172a;
  --text-secondary: #475569;
  --text-tertiary: #94a3b8;
  --text-inverse: #ffffff;

  /* Functional Colors */
  --color-success: #10b981;
  --color-success-bg: #ecfdf5;
  --color-error: #ef4444;
  --color-error-bg: #fef2f2;
  --color-warning: #f59e0b;
  --color-warning-bg: #fffbeb;
  --color-info: #0ea5e9;
  --color-info-bg: #f0f9ff;

  /* Spacing Scale (4px base unit) */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 12px;
  --spacing-lg: 16px;
  --spacing-xl: 20px;
  --spacing-2xl: 24px;
  --spacing-3xl: 32px;
  --spacing-4xl: 40px;

  /* Typography Scale */
  --font-sans: -apple-system, BlinkMacSystemFont, 'Inter', 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
  --font-mono: 'SF Mono', 'Monaco', 'Menlo', 'Consolas', monospace;

  --text-xs: 12px;
  --text-sm: 13px;
  --text-base: 14px;
  --text-lg: 16px;
  --text-xl: 18px;
  --text-2xl: 24px;
  --text-3xl: 32px;

  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  --line-height-tight: 1.25;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;

  /* Letter Spacing */
  --letter-spacing-tight: -0.5px;
  --letter-spacing-normal: -0.2px;
  --letter-spacing-wide: -0.1px;

  /* Border Radius */
  --radius-sm: 8px;
  --radius-md: 10px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-full: 9999px;

  /* Shadows */
  --shadow-xs: 0 1px 2px rgba(15, 23, 42, 0.04);
  --shadow-sm: 0 1px 3px rgba(15, 23, 42, 0.06), 0 1px 2px rgba(15, 23, 42, 0.04);
  --shadow-md: 0 4px 6px -1px rgba(15, 23, 42, 0.08), 0 2px 4px -1px rgba(15, 23, 42, 0.04);
  --shadow-lg: 0 10px 15px -3px rgba(15, 23, 42, 0.1), 0 4px 6px -2px rgba(15, 23, 42, 0.05);
  --shadow-accent: 0 2px 8px rgba(13, 148, 136, 0.25);
  --shadow-accent-hover: 0 4px 14px rgba(13, 148, 136, 0.35);

  /* Transitions */
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 200ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 300ms cubic-bezier(0.4, 0, 0.2, 1);

  /* Z-Index Scale */
  --z-dropdown: 1000;
  --z-modal: 1100;
  --z-tooltip: 1200;
}
```

Placement: Insert immediately after line 5 (after the * selector reset), before the body selector (line 7).
  </action>
  <verify>
Open popup/popup.css and verify:
1. CSS custom properties are defined immediately after the * selector
2. All color groups (primary, accent, backgrounds, text, functional) are present
3. Spacing scale starts at 4px (xs) with consistent 4px increments
4. Typography scale includes both font and text size variables
5. Border radius, shadows, and transitions are defined
Command: head -n 150 popup/popup.css | grep -E "(--color-|--spacing-|--font-|--text-|--radius-|--shadow-|--transition-)" | wc -l should return > 50
  </verify>
  <done>
Design system variables defined with color palette (deep blue/charcoal primary, teal accent), spacing scale (4px base), typography scale, and utility tokens (radius, shadows, transitions).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply design system variables to base styles and components</name>
  <files>popup/popup.css</files>
  <action>
Replace all hardcoded color, spacing, and typography values in popup/popup.css with CSS variables. Follow this mapping:

**Base Styles (body, .container):**
- body color: #1d1d1f → var(--text-primary)
- body background: #f5f5f7 → var(--bg-secondary)
- body font-family: Keep existing, add var(--font-sans) as first value
- body width: Keep 380px (DSGN-09: reasonable extension popup width)
- body min-height: Keep 500px (reasonable extension popup height)

**.container:**
- padding: 24px → var(--spacing-2xl)

**Header:**
- .header h1 color: #1d1d1f → var(--text-primary)
- .header border-bottom: rgba(0, 0, 0, 0.06) → rgba(15, 23, 42, 0.06) (using primary-900)
- .header margin-bottom: 24px → var(--spacing-2xl)
- .header padding-bottom: 16px → var(--spacing-lg)

**Status Section:**
- .status-section background: #ffffff → var(--bg-primary)
- .status-section border: rgba(0, 0, 0, 0.04) → rgba(15, 23, 42, 0.04)
- .status-section padding: 16px 18px → var(--spacing-lg) 18px
- .status-section margin-bottom: 20px → var(--spacing-xl)
- .status-section border-radius: 16px → var(--radius-xl)
- .status-section box-shadow: Current values → var(--shadow-sm)

**Status Message:**
- #statusText color: #6e6e73 → var(--text-secondary)
- #statusText font-size: 14px → var(--text-base)
- .status-message gap: 12px → var(--spacing-lg)

**Main Section (Ready mode):**
- .main-section background: #ffffff → var(--bg-primary)
- .main-section padding: 32px 24px → var(--spacing-3xl) var(--spacing-2xl)
- .main-section border-radius: 16px → var(--radius-xl)
- .main-section box-shadow: var(--shadow-sm)

**Tailor Button (PRIMARY ACTION):**
- .tailor-btn background: #007aff → var(--color-accent-600) (teal)
- .tailor-btn color: white → var(--text-inverse)
- .tailor-btn border-radius: 12px → var(--radius-lg)
- .tailor-btn padding: 16px 32px → var(--spacing-lg) var(--spacing-3xl)
- .tailor-btn font-size: 17px → var(--text-xl)
- .tailor-btn box-shadow: Current → var(--shadow-accent)
- .tailor-btn hover background: #0051d5 → var(--color-accent-700)
- .tailor-btn hover box-shadow: Current → var(--shadow-accent-hover)
- .tailor-btn disabled background: #d1d1d6 → var(--color-primary-300)
- .tailor-btn disabled color: #86868b → var(--color-primary-400)

**Hint Text:**
- .hint-text color: #86868b → var(--text-tertiary)
- .hint-text font-size: 13px → var(--text-sm)
- .hint-text margin-top: 8px → var(--spacing-sm)

**Secondary Buttons (Choose CV, Save API Key):**
- .secondary-btn background: #007aff → var(--color-accent-600)
- .secondary-btn border-radius: 10px → var(--radius-md)
- .secondary-btn padding: 11px 18px → var(--spacing-md) var(--spacing-lg)
- .secondary-btn font-size: 14px → var(--text-base)
- .secondary-btn box-shadow: Current → var(--shadow-xs)
- .secondary-btn hover background: #0051d5 → var(--color-accent-700)
- .secondary-btn hover box-shadow: var(--shadow-sm)

**Danger Button (Delete CV):**
- .danger-btn background: #ff3b30 → var(--color-error)
- .danger-btn box-shadow: var(--shadow-xs)
- .danger-btn hover background: #d32f2f → var(--color-error)

**Input Fields:**
- input[type="password"] background: #ffffff → var(--bg-primary)
- input[type="password"] color: #1d1d1f → var(--text-primary)
- input[type="password"] border: rgba(0, 0, 0, 0.12) → rgba(15, 23, 42, 0.12)
- input[type="password"] border-radius: 10px → var(--radius-md)
- input[type="password"] padding: 12px 14px → var(--spacing-lg) 14px
- input[type="password"] font-size: 14px → var(--text-base)
- input[type="password"] focus border-color: #007aff → var(--color-accent-600)
- input[type="password"] focus box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.1) → 0 0 0 3px rgba(13, 148, 136, 0.1)

**Icon Button (Settings):**
- .icon-btn background: #ffffff → var(--bg-primary)
- .icon-btn border: rgba(0, 0, 0, 0.08) → rgba(15, 23, 42, 0.08)
- .icon-btn color: #86868b → var(--text-tertiary)
- .icon-btn hover color: #1d1d1f → var(--text-primary)
- .icon-btn hover background: #f5f5f7 → var(--bg-secondary)
- .icon-btn border-radius: 50% → var(--radius-full)

**Close Button (Settings):**
- .close-btn background: #f5f5f7 → var(--bg-secondary)
- .close-btn color: #86868b → var(--text-tertiary)
- .close-btn hover background: #e5e5e7 → var(--color-primary-200)
- .close-btn hover color: #1d1d1f → var(--text-primary)
- .close-btn border-radius: 50% → var(--radius-full)

**Onboarding Progress:**
- .onboarding-progress background: #f5f5f7 → var(--bg-secondary)
- .onboarding-progress color: #1d1d1f → var(--text-primary)
- .onboarding-progress border: rgba(0, 0, 0, 0.04) → rgba(15, 23, 42, 0.04)
- .onboarding-progress border-radius: 10px → var(--radius-md)
- .onboarding-progress padding: 12px 16px → var(--spacing-lg) var(--spacing-lg)
- .onboarding-progress.complete background: #e8f5e9 → var(--color-success-bg)
- .onboarding-progress.complete color: #1b5e20 → var(--color-success)
- .onboarding-progress.complete border-color: #4caf50 → var(--color-success)

**Setting Groups:**
- .setting-group gap: 10px → var(--spacing-md)
- .setting-group label color: #1d1d1f → var(--text-primary)
- .setting-group label font-size: 14px → var(--text-base)
- .cv-status background: #f5f5f7 → var(--bg-secondary)
- .cv-status border: rgba(0, 0, 0, 0.04) → rgba(15, 23, 42, 0.04)
- .cv-status border-radius: 10px → var(--radius-md)
- .cv-status padding: 14px → var(--spacing-lg)
- .no-cv-text color: #86868b → var(--text-tertiary)
- .cv-info color: #6e6e73 → var(--text-secondary)
- .cv-info strong color: #1d1d1f → var(--text-primary)

**Help Text:**
- .help-text color: #86868b → var(--text-tertiary)
- .help-text font-size: 12px → var(--text-xs)
- .help-text a color: #007aff → var(--color-accent-600)
- .help-text a hover: Keep underline, color stays var(--color-accent-600)

**Message Section:**
- .message-section background: #ffffff → var(--bg-primary)
- .message-section border-radius: 14px → var(--radius-lg)
- .message-section padding: 14px 18px → var(--spacing-lg) 18px
- .message-section margin-top: 16px → var(--spacing-lg)
- #messageText color: #6e6e73 → var(--text-secondary)
- #messageText font-size: 13px → var(--text-sm)
- .success color: #34c759 → var(--color-success)
- .error color: #ff3b30 → var(--color-error)

**Result Section:**
- .result-section background: #ffffff → var(--bg-primary)
- .result-section border-radius: 16px → var(--radius-xl)
- .result-section padding: 24px → var(--spacing-2xl)
- .result-section margin-top: 16px → var(--spacing-lg)
- .result-section box-shadow: var(--shadow-md)
- .result-header h3 color: #1d1d1f → var(--text-primary)
- .result-header h3 font-size: 17px → var(--text-xl)
- .job-info color: #86868b → var(--text-tertiary)
- .pdf-info color: #34c759 → var(--color-success)
- .cover-letter-content background: #f5f5f7 → var(--bg-secondary)
- .cover-letter-content color: #1d1d1f → var(--text-primary)
- .cover-letter-content border-radius: 12px → var(--radius-lg)
- .cover-letter-content padding: 18px → var(--spacing-xl)
- .cover-letter-content margin-bottom: 18px → var(--spacing-xl)
- .cover-letter-content border: rgba(0, 0, 0, 0.04) → rgba(15, 23, 42, 0.04)

**Primary Button (Download PDF):**
- .primary-btn background: #007aff → var(--color-accent-600)
- .primary-btn border-radius: 10px → var(--radius-md)
- .primary-btn padding: 11px 18px → var(--spacing-md) var(--spacing-lg)
- .primary-btn font-size: 14px → var(--text-base)
- .primary-btn box-shadow: var(--shadow-xs)
- .primary-btn hover background: #0051d5 → var(--color-accent-700)
- .primary-btn hover box-shadow: var(--shadow-sm)
- .primary-btn disabled background: #d1d1d6 → var(--color-primary-300)
- .primary-btn disabled color: #86868b → var(--color-primary-400)

**Mode Headers:**
- .mode-header h2, .ready-header h2 color: #1d1d1f → var(--text-primary)
- .mode-header h2, .ready-header h2 font-size: 20px → var(--text-2xl)
- .mode-header, .ready-header border-bottom: rgba(0, 0, 0, 0.06) → rgba(15, 23, 42, 0.06)

**Section Cards:**
- .onboarding-section, .settings-section, .ready-section background: #ffffff → var(--bg-primary)
- .onboarding-section, .settings-section, .ready-section border-radius: 16px → var(--radius-xl)
- .onboarding-section, .settings-section, .ready-section padding: 24px → var(--spacing-2xl)
- .onboarding-section box-shadow: var(--shadow-sm)
- .settings-section, .ready-section box-shadow: var(--shadow-md)

**Step Instruction:**
- .step-instruction color: #1d1d1f → var(--text-primary)
- .step-instruction font-size: 14px → var(--text-base)
- .step-number color: #007aff → var(--color-accent-600)

**Step Number:**
- .step-number color: #007aff → var(--color-accent-600)

**All transitions:**
- Replace cubic-bezier(0.4, 0, 0.2, 1) → var(--transition-base)

**All margins/paddings:**
- Replace any remaining hardcoded values with nearest var(--spacing-*) variable

**All font sizes:**
- Replace any remaining hardcoded font-size values with var(--text-*) variables

**All font weights:**
- Replace numeric font-weight values with var(--font-weight-*) variables

**All letter-spacing:**
- Replace letter-spacing values with var(--letter-spacing-*) variables

**Important:** Do NOT change the popup dimensions (380px width, 500px min-height) as specified in DSGN-09.
  </action>
  <verify>
Open popup/popup.css and verify:
1. Hardcoded hex colors are minimized (should see var(--color-*) and var(--bg-*) throughout)
2. Spacing uses var(--spacing-*) variables
3. Font sizes use var(--text-*) variables
4. Typography uses var(--font-*) variables
5. Primary action (Tailor button) uses accent color: var(--color-accent-600)
6. Status message text uses var(--text-secondary)
7. Help text links use var(--color-accent-600)
8. All transitions use var(--transition-base)
9. Popup body width is still 380px (not changed)
Command: grep -o "var(--color-[^)]*)" popup/popup.css | wc -l should return > 30
Command: grep -o "var(--spacing-[^)]*)" popup/popup.css | wc -l should return > 20
Command: grep -o "var(--text-[^)]*)" popup/popup.css | wc -l should return > 15
  </verify>
  <done>
All hardcoded colors, spacing, and typography values replaced with CSS variables. Design system is now consistent throughout the stylesheet with deep blue/charcoal primary colors, teal accent color, and proper visual hierarchy.
  </done>
</task>

</tasks>

<verification>
After completing both tasks, verify the design system implementation:

1. **Color System Check:**
   - Open popup/popup.css in browser DevTools or editor
   - Search for "var(--color-accent-600)" - should find multiple occurrences (Tailor button, links, focus states)
   - Search for "var(--text-primary)" - should be used for headings and primary text
   - Verify no hardcoded teal/blue (#007aff, #0051d5) remain (grep should return 0)

2. **Spacing Consistency Check:**
   - Grep for var(--spacing-*) - should show consistent 4px-based scale usage
   - Margins and paddings follow 4px grid (xs: 4px, sm: 8px, md: 12px, lg: 16px, etc.)

3. **Typography System Check:**
   - Verify all font-size values use var(--text-*) variables
   - Check that headings use larger text sizes (text-xl, text-2xl)
   - Verify body text uses text-base (14px) or text-sm (13px)
   - Confirm letter-spacing uses var(--letter-spacing-*) variables

4. **Visual Hierarchy Check:**
   - Primary action (Tailor button) is visually prominent (accent color, larger padding)
   - Secondary actions (Save API Key, Choose CV) are less prominent but clear
   - Help text is tertiary (smaller, lighter color)
   - Section headers have proper visual weight

5. **Popup Dimensions Check:**
   - Body width is 380px (reasonable for Chrome extension)
   - Body min-height is 500px (reasonable for Chrome extension)

6. **Design Aesthetic Check:**
   - Overall feel should be professional and trustworthy
   - Colors are not overly bright (deep blue/charcoal, teal)
   - Generous whitespace between elements
   - Clean borders and subtle shadows
</verification>

<success_criteria>
1. All UI elements use deep blue/charcoal as primary color (var(--color-primary-*))
2. Accent color is teal (var(--color-accent-600): #0d9488)
3. Background is white (var(--bg-primary): #ffffff) or light gray (var(--bg-secondary): #f9fafb)
4. Design has clear visual hierarchy with primary actions emphasized (Tailor button uses accent color, larger padding)
5. Layout uses generous whitespace for readability (consistent spacing scale)
6. Design is clean and professional (Linear meets modern job platform aesthetic - subtle shadows, clean borders, refined typography)
7. Typography uses modern sans-serif font (var(--font-sans): Inter + system fonts)
8. Popup dimensions remain reasonable (380px width, 500px min-height)
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-design-system/02-01-SUMMARY.md`
</output>
